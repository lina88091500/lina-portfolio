# 作品集說明

這份文件說明根目錄 `index.html`（作品集首頁）的設計方式，以及四個作品資料夾各自是什麼、怎麼打開來看。

---

## 一、整體想法

作品集首頁只做一件事：**讓看的人在很短時間內看懂「會什麼、做過什麼」，點一下就能開啟作品。**

首頁由上往下分成三塊：

1. **你是誰** —— 姓名、一句話定位、一段不誇張的自我介紹
2. **會什麼** —— 技能標籤，而且都是真的用在下面四個作品裡的技能
3. **做過什麼** —— 四張作品卡片，點了會在新視窗開啟該專案，方便一邊看首頁一邊比對

沒有多餘的動畫、沒有空話，乾淨、好讀、資訊到位。

---

## 二、首頁 `index.html` 的設計

### 版面

- 單欄、置中、最寬 1040px。內容由上往下讀，不需要左右張望；手機打開會自動變成單欄，標籤和卡片自動換行。
- 每個區塊之間用一條細線隔開，像雜誌排版一樣讓段落分明。
- 每張作品卡片是一個完整的資訊單位：類型 → 標題 → 資料夾名稱 → 說明 → 用到的技術 → 「開啟專案 →」。整張卡片都是連結，用 `target="_blank"` 在新視窗開啟，滑鼠移上去會微微浮起提示「可以點」。

### 配色

只用三個顏色：

| 角色 | 色碼 | 用在哪 |
|---|---|---|
| 背景 | `#faf7f2`（暖米白） | 整個頁面底色 |
| 文字 | `#2c2a28`（深墨） | 內文，比純黑柔和 |
| 重點色 | `#9c5c68`（灰調玫瑰） | 標題小字、連結、卡片 hover 邊框 |

重點色選「灰調玫瑰」，同時呼應兩組作品的調性（Bloom 美妝的粉、ZL& 飾品的香檳金），但把彩度壓低，看起來沉穩。顏色越少，畫面越有秩序感。

### 字體

- 標題：**Noto Serif TC（思源宋體）**，襯線字帶一點正式與質感。
- 內文：**Noto Sans TC（思源黑體）**，螢幕閱讀性好。
- 兩套都是 Google Fonts 上的免費開源字體。


### 技能標籤

分成「網頁前端」「後端與資料庫」「視覺設計」「開發協作」「語言」五組，其中「開發協作」包含 Git、GitHub，以及輔助開發用的 Claude、ChatGPT、Gemini。

---

## 三、四個作品

### project-a ｜ ZL& accessories 飾品電商

- **類型：** 響應式靜態網站（純前端，不需要伺服器）
- **技術：** HTML / CSS / JavaScript ＋ Bootstrap 5
- **頁面：** 首頁、商品總覽（分類篩選＋動態分頁）、商品詳情頁（依網址參數 `?id=1~8` 顯示對應商品）、會員登入／註冊、專屬訂製需求表單
- **看點：** 版面設計完整、手機版另外調過、用 JavaScript 做了商品篩選、分頁、收藏、登入狀態切換等互動
- **登入示範：** 首頁與商品頁都有提示——點右上「登入」進入會員中心，輸入**任意 Email 與任意密碼**即可登入（純前端展示，登入狀態存在瀏覽器 localStorage）

### project-b ｜ 尋找貓咪的家 · 領養平台

- **類型：** 前後台介面原型（純前端，靜態頁面）
- **技術：** HTML / CSS ＋ Bootstrap 5 ＋ JavaScript
- **前台：** 輪播精選貓咪、待領養列表、貓咪詳情頁
- **後台：** 完整的「列表 → 新增 → 修改 → 已被收養（刪除）」流程。這四個動作會真的讀寫同一份資料——共用的 `hw/cats-data.js` 把貓咪清單存在瀏覽器 localStorage 裡，新增/修改/刪除都是對同一份清單操作，重新整理後台列表也看得到結果；上傳的照片會直接轉成圖片存起來（純前端展示，沒有後端伺服器可以真的存檔案）
- **權限：** 未登入不能用網址直接闖入後台或新增／修改／刪除頁，會被導回登入頁；登出則會直接回到前台首頁
- **登入示範：** 點右上「登入」，帳號 `admin`、密碼 `1234`（純前端展示，實際輸入任意帳密皆可登入）

### project-c ｜ Bloom Aesthetic Studio 日系美妝 CMS · 固定版面版

- **類型：** PHP ＋ MySQL 動態網站（前台＋後台內容管理），以網頁設計乙級檢定題目為基礎改作
- **版面：** 整個網站固定 1024px 寬，是這套系統原本的規格
- **後台功能：** 管理網站標題與 Header 圖、動態文字廣告、動畫圖片、校園（品牌）映象資料、進站總人數、頁尾文字、最新消息、導覽選單與子選單、管理員帳號
- **登入示範：** 點「品牌後台管理」，帳號 `admin`、密碼 `1234`（未匯入資料庫也能登入瀏覽）

### project-d ｜ Bloom Aesthetic Studio 日系美妝 CMS · RWD 版面版

- **類型：** 與 project-c 完全同一套後端邏輯與資料庫結構，前台重寫為流動式響應式版面
- **版面：** 新的置頂導覽列、可捲動的選單、依裝置寬度自動調整的三欄／單欄佈局，手機、平板、桌機都能正常閱讀；後台管理介面同樣是響應式的，寬螢幕下選單與內容面板置中排列
- **後台功能：** 與 project-c 相同
- **登入示範：** 與 project-c 相同

作品集首頁把 project-c 與 project-d 並排在同一個標題「Bloom Aesthetic Studio · 同一套系統的兩種版面」下面，讓看的人一眼看出「這是同一套系統，做了固定版面與響應式兩種呈現」。

---

## 四、共通的設計重點

四個作品都有以下共通點：

1. **每個網站都有分頁小圖示（favicon）**：project-a 用 💍、project-b 用 🐾、project-c／project-d 用 🌸，瀏覽器分頁不會顯示空白圖。
2. **登入示範一律標示「帳密使用登入號」**，而不是寫成僅供特定對象的示範文字，讓任何看網站的人都清楚該輸入什麼。
3. **project-c／project-d 連不到資料庫也能看**：資料庫類別 `api/db.php` 只嘗試連線一次、9 個資料表共用同一個連線、加上 2 秒逾時，連不到資料庫時各頁面會自動改用內建的範例資料撐滿畫面，展示模式下約 2 秒內就能看到內容。
4. **project-c／project-d 的資料庫安全性**：所有寫進 SQL 的值都先經過 `PDO::quote()` 逃逸處理；管理員密碼用 bcrypt（`password_hash` / `password_verify`）雜湊儲存，若資料庫裡還留著舊的明碼密碼，登入成功的當下會自動升級成雜湊。
5. **後台都有登入守衛**：project-b 的 `admin.html` 與新增／修改／刪除頁、project-c／project-d 的 `admin.php`，未登入時打開網址都會被導回登入頁；四個後台的登出都會直接送回各自的前台首頁，不會卡在登入頁面。

---

## 五、怎麼打開來看

### 線上直接看

整個作品集資料夾已經用 FileZilla 上傳到虛擬主機，不需要另外裝任何環境，直接開瀏覽器就能看：

**http://220.128.133.15/s1150101/lina/index.html**

這個網址開啟的就是作品集首頁本身，點首頁上的四張作品卡片會在新視窗分別開啟 project-a、project-b、project-c、project-d（含 project-c、project-d 的 PHP 頁面）。

### 本機開啟

| 專案 | 怎麼看 |
|---|---|
| 作品集首頁 | 直接用瀏覽器開啟根目錄的 `index.html` |
| project-a、project-b | 直接用瀏覽器開啟資料夾裡的 `index.html` |
| project-c、project-d | 需要 PHP 環境（例如 **XAMPP**）。把整個資料夾放進 `htdocs`，瀏覽器開 `http://localhost/project-c/index.php`（project-d 同理） |

- project-c／project-d **沒有匯入資料庫也能看**，會顯示範例資料。
- 想看真實資料庫版：用 phpMyAdmin 匯入資料夾裡的 `db.sql`，或執行 `import.php`。
- 各專案首頁上都標示了帳密使用登入號，內容見上方「四、四個作品」。

### 想放上網（GitHub Pages）

- 純前端的 project-a、project-b 和作品集首頁可以直接放 GitHub Pages。
- project-c、project-d 是 PHP，GitHub Pages **不支援**，需要找支援 PHP 的免費主機（例如 InfinityFree、000webhost），或現場用自己電腦的 XAMPP 展示。
- 上 GitHub Pages 後，作品集首頁的四張卡片連結（`./project-a/index.html` 等）會自動生效。

---

## 六、之後可以再加強的方向

不是現在的問題，是未來可以練習的方向：

- **project-a：** 商品「收藏愛心」目前只有詳情頁能用，列表頁的愛心還沒接功能，可以補上。
- **project-b：** 後台資料目前存在瀏覽器 localStorage，前台首頁與詳情頁的貓咪資料則是另外寫死的展示內容，兩邊還沒串在一起；之後可以把整個前後台接成同一份真正會存檔的資料。
- **project-c / project-d：** 資料庫層可以練習改寫成預備語句（prepared statements）；上傳圖片時可以加上副檔名與檔案類型檢查。
- **共通：** 四個專案的圖片都可以再優化檔案大小，載入會更快。

---

*最後更新：2026-09-03*
