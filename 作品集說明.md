# 作品集說明：設計理念與作法

這份文件說明「根目錄 `index.html` 作品集首頁」是怎麼設計的，以及四個作品資料夾各自被整理了哪些地方、為什麼要這樣改。用字盡量白話，沒有程式基礎也能看懂。

---

## 一、整體想法

你手上有四個練習專案，程度和類型都不一樣。作品集首頁的任務只有一個：**讓看的人（面試官）在 30 秒內看懂「你會什麼、做過什麼」，然後點一下就能進到作品裡。**

所以首頁只放三塊東西，順序也是照面試官的閱讀習慣排的：

1. **你是誰**（姓名、一句話定位、一段不誇張的自我介紹）
2. **你會什麼**（技能標籤，而且是「真的用在下面作品裡」的技能）
3. **你做過什麼**（四張作品卡片，點了直接開啟）

沒有多餘的動畫、沒有「歡迎來到我的世界」這種空話。面試官時間很少，乾淨、好讀、資訊到位，比華麗更有專業感。

---

## 二、首頁 `index.html` 的設計決定

### 版面

- **單欄、置中、最寬 1040px。** 內容由上往下讀，不需要左右張望。手機打開會自動變成單欄，標籤和卡片會自己換行。
- **每個區塊之間用一條細線隔開。** 這是雜誌、報章的作法，讓「一段落一段落」很清楚，比用一堆色塊分區更耐看。
- **卡片式作品區。** 每張卡片是一個完整的資訊單位：類型 → 標題 → 資料夾名稱 → 兩三句說明 → 用到的技術 → 「開啟專案 →」。整張卡片都是連結，滑鼠移上去會微微浮起來，暗示「這可以點」。

### 配色

只用三個顏色，這是刻意的：

| 角色 | 色碼 | 用在哪 |
|---|---|---|
| 背景 | `#faf7f2`（暖米白） | 整個頁面底色 |
| 文字 | `#2c2a28`（深墨） | 內文，不用純黑，比較柔和 |
| 重點色 | `#9c5c68`（灰調玫瑰） | 標題小字、連結、卡片 hover 邊框 |

重點色選「灰調玫瑰」，是因為它同時呼應你兩組作品的調性（Bloom 美妝的粉、ZL& 飾品的香檳金），但把彩度壓低，看起來沉穩、不會太甜。**顏色越少，畫面越有秩序感。**

### 字體

- 標題用 **Noto Serif TC（思源宋體）**：有襯線的宋體帶一點正式和質感。
- 內文用 **Noto Sans TC（思源黑體）**：黑體在螢幕上好讀。
- 這兩套都是 Google Fonts 上的免費開源字體，任何人打開都會正確顯示。

### 文案語氣

自我介紹只寫「做過的事實」，不寫形容詞轟炸。例如寫「獨立負責宣傳圖文、形象照修圖、影片剪輯與每月月會報告」，而不是「精通各種設計、學習能力超強」。**具體的事實比誇大的形容詞可信。**

---

## 三、四個作品分別是什麼

### project-a ｜ ZL& accessories 飾品電商

- **類型：** 響應式靜態網站（純前端，不需要伺服器）
- **技術：** HTML / CSS / JavaScript ＋ Bootstrap 5
- **內容：** 首頁、商品總覽（分類篩選＋分頁）、商品詳情頁、會員登入／註冊、專屬訂製表單
- **看點：** 版面設計完整、手機版有另外調過、用 JavaScript 做了篩選、分頁、收藏、登入狀態切換

**這次整理了什麼：**

| 項目 | 原本狀況 | 改成 |
|---|---|---|
| 商品詳情頁 | 不管從哪個商品點進去，永遠顯示同一個「戒指」 | 改成讀取網址參數 `?id=1~8`，正確顯示對應的商品名稱、價格、材質、故事、規格 |
| 商品列表計數 | 把「諮詢表單」也算成一件商品，顯示數量會多一個 | 表單不再列入商品計數與分頁 |
| Instagram 圖示 | SVG 路徑有一個多餘的符號，圖示破圖 | 修正路徑，圖示正常顯示 |
| 字體 | CSS 指定了 `Cinzel` 字體但沒有載入，只吃到系統預設 | 補上 Google Fonts 載入，畫面和當初設計一致 |
| 網站小圖示（favicon） | 沒有，瀏覽器分頁顯示空白圖 | 五個頁面都加上 💍 圖示 |
| Bootstrap 版本 | `custom.html` 用 5.3.0，其他頁用 5.3.8 | 全部統一成 5.3.8 |
| 詳情頁文字 | 保養說明裡夾雜一個英文字「wiping」 | 移除 |

### project-b ＋ project-c ｜ Bloom Aesthetic Studio 日系美妝 CMS

這兩個是**同一套系統的兩個版本**，以網頁設計乙級檢定題目為基礎改作：

- **project-b：** 固定版面版。整個網站固定 1024px 寬，是檢定原本的規格。
- **project-c：** 響應式重構版。後端邏輯一樣，但前台版面整個重寫成「會跟著螢幕寬度伸縮」的流動式版面，多了新的置頂導覽列、可捲動選單、手機／平板／桌機自動切換佈局。

作品集首頁把它們放在同一個標題底下、並排兩張卡片，讓面試官看到「這是同一個東西，我做了兩種版面」，這本身就是一個可以講的故事——**從固定版面進化到響應式**。

- **類型：** PHP ＋ MySQL 動態網站（前台＋後台內容管理）
- **後台功能：** 管理網站標題、輪播圖、最新消息、導覽選單與子選單、管理員帳號、頁尾文字、訪客人數
- **貼心設計：** 連不到資料庫時，網站不會壞掉，會自動改用內建的「範例資料」把畫面撐完整——這樣沒有架資料庫的人也能直接看到成果。

**這次整理了什麼（兩個版本共用同一批修正）：**

| 項目 | 原本狀況 | 改成 |
|---|---|---|
| SQL 資料庫查詢 | 直接把使用者輸入拼進 SQL 字串，有 SQL Injection（資料庫注入攻擊）風險 | 所有寫進 SQL 的值都先經過 `PDO::quote()` 逃逸處理，堵住注入漏洞；並在連不到資料庫時安全略過 |
| 管理員密碼 | 資料庫裡直接存明碼 `1234` | 改用 bcrypt 雜湊儲存；登入改用 `password_verify()` 比對；若資料庫還留著舊明碼，登入成功時會自動幫它升級成雜湊 |
| 新增／修改帳號 | 送出的密碼原封不動存進資料庫 | 新增時自動雜湊；修改時「留空＝不變更」，有填才重新雜湊 |
| 後台權限 | 直接在網址打 `admin.php` 就能進後台，`$_SESSION` 存了卻沒人檢查 | `admin.php` 加上守衛：未登入自動導回登入頁；登出按鈕改成 `?do=logout`，會真正清除 session；展示模式（無資料庫）下用 `admin` / `1234` 仍可登入瀏覽 |
| `api/login.php` | 檔案結尾殘留一行測試用的 `echo "HI";` | 移除，並整理成正常的驗證流程 |
| 後台「總人數」「頁尾文字」存檔 | 表單送到一個不存在的檔案 `api/edit_info.php`，按了沒反應 | 改指向實際存在的 `api/edit_value.php`，並補上 `id` 讓它正確「更新」而不是「新增一筆」 |
| project-b 的頁首橫幅圖 | 預設圖檔名 `top_ad_banner.svg` 在資料夾裡根本不存在，破圖 | 改成資料夾裡實際有的 `header_jbeauty_v2.png` |
| 網站小圖示（favicon） | 沒有 | 前後台都加上 🌸 圖示 |
| 多餘檔案 | `11501-BQUIZ01-01`（沒有副檔名的舊檢定存檔） | 移除（`index.php` 才是正式版本；此檔內容已在 git 歷史保留） |

> ⚠️ 關於 SQL 安全性：更徹底的做法是把整個資料庫類別改寫成「預備語句（prepared statements）」。這次選擇用 `PDO::quote()`，是因為它同樣能堵住注入漏洞，又不會動到你原本能正常運作的檢定程式結構、風險比較低。之後有時間可以再往預備語句重構。

### project-d ｜ 尋找貓咪的家 · 領養平台

- **類型：** 後台介面原型（純前端，靜態頁面）
- **內容：** 前台輪播＋待領養列表＋詳情頁；後台「列表 → 新增 → 修改 → 刪除」完整流程頁面
- **看點：** 這個作品的重點是「一套後台 CRUD 流程長什麼樣子」，介面設計乾淨、每一步都有對應頁面

**這次整理了什麼：**

| 項目 | 原本狀況 | 改成 |
|---|---|---|
| 後台權限 | 直接在網址打 `admin.html` 就能進後台，登入形同虛設 | 後台與新增／修改／刪除頁面都加上守衛：未登入會跳提示並自動導回登入頁 |
| 登入頁程式 | 有幾行宣告了卻沒用到的變數、變數重複宣告 | 清乾淨，並加上註解說明「這是純前端示範，實務要送後端驗證」 |
| 詳情頁文字 | 貓咪介紹寫成「溫慢的大乖乖」 | 修正為「溫柔的大乖乖」 |
| 導覽列連結 | 「志工夥伴」連結是空的 `href=""`，點了會重新整理頁面 | 改成 `href="#"`，不會誤觸重整 |
| 網站小圖示（favicon） | 沒有 | 全部頁面加上 🐾 圖示 |

---

## 四、這次共通的整理原則

不管哪個專案，這幾件事都有做：

1. **每個網站都補上分頁小圖示（favicon）。** 沒有 favicon 的網站，瀏覽器分頁會是一片空白，看起來像沒做完。
2. **把「明顯壞掉、會出錯、或沒寫完」的地方修好。** 破圖、殘留的測試碼、點了沒反應的按鈕、指向不存在檔案的表單。
3. **不改動整體架構。** 這些是你熟悉的檔案，維持你看得懂的結構，只在「壞掉的點」上動刀。
4. **能安全加強的就加強。** 例如資料庫注入防護、密碼雜湊、後台權限守衛——這些是面試時可以講的加分項。
5. **改動都寫在這份文件裡。** 這樣你自己回頭看、或被面試官問到，都答得出「我改了什麼、為什麼」。

---

## 五、怎麼打開來看

| 專案 | 怎麼看 |
|---|---|
| 作品集首頁 | 直接用瀏覽器開啟根目錄的 `index.html` |
| project-a、project-d | 直接用瀏覽器開啟資料夾裡的 `index.html` |
| project-b、project-c | 需要 PHP 環境（例如 **XAMPP**）。把整個資料夾放進 `htdocs`，瀏覽器開 `http://localhost/project-b/index.php` |

- project-b／c **沒有匯入資料庫也能看**，會顯示範例資料。
- 想看真實資料庫版：用 phpMyAdmin 匯入資料夾裡的 `db.sql`，或執行 `import.php`。
- 後台登入：帳號 `admin`、密碼 `1234`。

### 想放上網（GitHub Pages）

- 你的履歷有提到會用 Git／GitHub Pages。**純前端的 project-a、project-d 和這份首頁可以直接放 GitHub Pages。**
- project-b、project-c 是 PHP，GitHub Pages **不支援**，需要找支援 PHP 的免費主機（例如 InfinityFree、000webhost），或在面試時用自己電腦的 XAMPP 展示。
- 上 GitHub Pages 後，作品集首頁的四張卡片連結（`./project-a/index.html` 等）會自動生效。

---

## 六、之後可以再加強的方向

不是現在的問題，是給你未來練習的方向：

- **project-a：** 商品「收藏愛心」目前只有詳情頁能用，列表頁的愛心還沒接功能，可以補上。
- **project-b / c：** 資料庫層可以練習改寫成預備語句（prepared statements）；上傳圖片時可加上副檔名與檔案類型檢查。
- **project-d：** 目前資料是寫死在 JavaScript 裡的，之後學會 PHP＋資料庫後，可以把它接成真正會存檔的後台。
- **共通：** 四個專案的圖片都可以再優化大小，載入會更快。

---

*最後更新：2026-08-31*
