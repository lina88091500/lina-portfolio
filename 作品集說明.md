# 作品集說明

這份文件說明根目錄 `index.html`（作品集網站）的設計方式、四個作品各自是什麼，以及怎麼打開來看。

---

## 一、整體想法

作品集要做到三件事：**讓看的人在很短時間內看懂「會什麼、做過什麼」、看得出「怎麼做的」，而且不用一直往下捲。**

所以網站不是一頁到底，而是做成 **分頁式（單頁應用 SPA）**：

- 頂端有六個按鈕：**首頁 / 關於我 / 技能 / 作品 / 學習歷程 / 聯絡我**
- 點按鈕直接切換內容，不會整頁重新載入，網址會變成 `index.html#/works` 這種形式
- 四個作品各有一個**專案細節頁**（`#/work/a` ～ `#/work/d`），寫清楚目標、流程、我負責的部分與成果

---

## 二、網站結構

| 分頁 | 網址 | 內容 |
|---|---|---|
| 首頁 | `#/home` | 主視覺、作品截圖拼貼、四個數字（4 個專案／5 個 GitHub 練習庫／8 年經驗／2026 結訓）、精選作品三張卡、怎麼看這份作品集 |
| 關於我 | `#/about` | 擅長什麼、正在補強什麼、做事方式；轉職歷程時間軸；這個作品集網站本身的設計說明 |
| 技能 | `#/skills` | 六組技能卡，深色標籤＝最有把握的項目，每組都標註「用在哪個作品」 |
| 作品 | `#/works` | 四張作品卡（含實際畫面截圖），可進細節頁或直接開啟作品 |
| 專案細節 | `#/work/a`～`d` | 每個作品的完整說明，見下方「三」 |
| 學習歷程 | `#/learning` | 五個課堂練習的 GitHub repo |
| 聯絡我 | `#/contact` | Email、電話、所在地、GitHub、展示帳號、線上版本網址 |

### 專案細節頁的固定架構

每個作品都用同一套架構寫，讓看的人不用發問就能理解全貌：

1. **畫面截圖**（project-a 與 project-d 各有兩張，可以直接對照）
2. **這個專案在做什麼** —— 為什麼做這個 ／ 我負責的部分 ／ 做完之後學到的
3. **製作流程** —— 五個步驟，寫實際的順序
4. **做出什麼** —— 具體、可以在網站上點開驗證的成果
5. **展示帳號與下一步** —— 帳密，以及誠實列出還沒做完的部分

### 版面與配色

- 內容置中、最寬 1120px，手機、平板、桌機都調過；分頁切換有淡入動畫。
- 只用三個主要顏色：背景 `#faf7f2`（暖米白）、文字 `#2c2a28`（深墨）、重點色 `#9c5c68`（灰調玫瑰），另有輔助的霧金 `#b8895f`。重點色同時呼應 Bloom 美妝的粉與 ZL& 飾品的香檳金。
- 標題用 **Noto Serif TC（思源宋體）**，內文用 **Noto Sans TC（思源黑體）**，皆為 Google Fonts 免費字體。
- 技術上是純 HTML / CSS / 原生 JavaScript，沒有用框架；四個作品的資料集中寫在 `index.html` 底部的 `WORKS` 陣列，卡片與細節頁都由同一份資料產生，改一處就會兩邊同步。
- 分工說明：版面、配色與文案由本人規劃，程式碼以 AI 工具輔助產生，再由本人調整與測試。網站頁尾與「關於我」都據實標示，不寫成純手刻。

### 截圖

`assets/shots/` 放的是四個作品的實際畫面截圖：

| 檔案 | 內容 |
|---|---|
| `project-a.png` | 飾品電商首頁 |
| `project-a-products.png` | 商品總覽頁（分類篩選＋分頁） |
| `project-b.png` | 領養平台前台首頁 |
| `project-c.png` | 日系美妝 CMS 固定版面版首頁 |
| `project-d.png` | RWD 版桌機畫面 |
| `project-d-mobile.png` | RWD 版手機畫面 |

---

## 三、四個作品

### project-a ｜ ZL& accessories 飾品電商

- **狀態：** 設計進行中，持續精修
- **緣由：** 朋友正在做斜槓賣飾品，想做一個她真的能拿來用的網站雛形，而不只是交作業
- **類型：** 響應式靜態網站（純前端，不需要伺服器）
- **技術：** HTML / CSS / JavaScript ＋ Bootstrap 5
- **頁面：** 首頁、商品總覽（分類篩選＋動態分頁）、商品詳情頁（依網址參數 `?id=1~8` 顯示對應商品）、會員登入／註冊、專屬訂製需求表單
- **看點：** 版面設計完整、手機版另外調過、用 JavaScript 做了商品篩選、分頁、收藏、登入狀態切換等互動
- **登入示範：** 點右上「登入」，輸入**任意 Email 與任意密碼**即可登入（純前端展示，登入狀態存在 localStorage）

### project-b ｜ 尋找貓咪的家 · 領養平台

- **類型：** 前後台介面原型（純前端，靜態頁面）
- **技術：** HTML / CSS ＋ Bootstrap 5 ＋ JavaScript
- **前台：** 輪播精選貓咪、待領養列表、貓咪詳情頁
- **後台：** 完整的「列表 → 新增 → 修改 → 已被收養（刪除）」流程。四個動作讀寫同一份資料 —— 共用的 `hw/cats-data.js` 把貓咪清單存在 localStorage，重新整理列表也看得到結果；上傳的照片會直接轉成圖片存起來
- **權限：** 未登入不能用網址直接闖入後台或新增／修改／刪除頁；登出直接回到前台首頁
- **登入示範：** 帳號 `admin`、密碼 `1234`（純前端展示，任意帳密皆可登入）

### project-c ｜ Bloom Aesthetic Studio · 固定版面版

- **類型：** PHP ＋ MySQL 動態網站（前台＋後台內容管理），以網頁設計乙級檢定題目為基礎改作
- **版面：** 前台固定 1024px 寬，是這套系統原本的規格
- **後台功能：** 網站標題與 Header 圖、動態文字廣告、動畫圖片、品牌形象資料、進站總人數、頁尾文字、最新消息、導覽選單與子選單、管理員帳號
- **登入示範：** 點「品牌後台管理」，帳號 `admin`、密碼 `1234`（未匯入資料庫也能登入瀏覽）

### project-d ｜ Bloom Aesthetic Studio · RWD 版面版

- **類型：** 與 project-c 完全同一套後端邏輯與資料庫結構，前台重寫為流動式響應式版面
- **版面：** 置頂導覽列、可捲動的選單、依裝置寬度自動調整的三欄／單欄佈局；後台管理介面同樣是響應式的
- **後台功能：** 與 project-c 相同
- **登入示範：** 與 project-c 相同

---

## 四、共通的設計重點

1. **每個網站都有分頁小圖示（favicon）**：project-a 用 💍、project-b 用 🐾、project-c／project-d 用 🌸。
2. **登入示範一律標示在網站頂端**，讓任何人都清楚該輸入什麼。
3. **project-c／project-d 連不到資料庫也能看**：`api/db.php` 只嘗試連線一次、9 個資料表共用同一個連線、加上 2 秒逾時；連不到時各頁面自動改用內建範例資料，約 2 秒內就能看到完整內容。
4. **project-c／project-d 的資料庫安全性**：寫進 SQL 的值都先經過 `PDO::quote()` 逃逸；管理員密碼用 bcrypt 雜湊儲存，若資料庫裡還留著舊的明碼密碼，登入成功當下會自動升級成雜湊。
5. **後台都有登入守衛**：未登入時用網址直接開後台會被導回登入頁；登出一律回到各自的前台首頁。

---

## 五、怎麼打開來看

### 線上直接看

**http://220.128.133.15/s1150101/lina/index.html**

不需要安裝任何環境，四個作品（含 PHP 的 project-c、project-d）都能直接點開。

### 本機開啟

| 專案 | 怎麼看 |
|---|---|
| 作品集網站 | 直接用瀏覽器開啟根目錄的 `index.html` |
| project-a、project-b | 直接用瀏覽器開啟資料夾裡的 `index.html` |
| project-c、project-d | 需要 PHP 環境（例如 **XAMPP**）。把整個資料夾放進 `htdocs`，開 `http://localhost/project-c/index.php` |

- project-c／project-d **沒有匯入資料庫也能看**，會顯示範例資料。
- 想看真實資料庫版：用 phpMyAdmin 匯入資料夾裡的 `db.sql`，或執行 `import.php`。
- project-c／project-d 需要 PHP 的 **mbstring** 擴充套件（XAMPP 預設已開啟）。

### 想放上網（GitHub Pages）

- 純前端的作品集網站、project-a、project-b 可以直接放 GitHub Pages。
- project-c、project-d 是 PHP，GitHub Pages **不支援**，需要找支援 PHP 的免費主機，或現場用 XAMPP 展示。

---

## 六、之後可以再加強的方向

- **project-a：** 商品列表頁的「收藏愛心」還沒接功能（詳情頁可以用），可以補上。
- **project-b：** 後台資料存在 localStorage，前台首頁與詳情頁的貓咪則是另外寫死的展示內容，兩邊還沒串在一起。
- **project-c / project-d：** 資料庫層可以改寫成預備語句（prepared statements）；上傳圖片時可以加上副檔名與檔案類型檢查。
- **作品集網站：** 之後有真實專案（有流量、轉換率等數據）時，補進「做出什麼」那一段會更有說服力。
- **共通：** 各專案的圖片可以再優化檔案大小，載入會更快。

---

*最後更新：2026-09-06*
